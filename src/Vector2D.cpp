#include "Vector2D.hpp"

Vector2D::Vector2D(){
  x = 0.0f;
  y = 0.0f;
}

Vector2D::Vector2D(float x, float y){
  // same as x = x, just using this to make it easier to read
  this->x = x;
  this->y = y;
}

Vector2D& Vector2D::Add(const Vector2D& vec){
  
}
